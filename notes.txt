- prompt structering : prompt repository , prompt version contol , prompt evaluation, llm integration.
-function load vector store
-add to db not based on collection name	
-test best embedding models
-Add structured metadata filtering (later when needed)
-Add language detector(depends on the embedding system)
------------------------------------
Database -> laws
   Collection -> pdf file (pdf laws for each case)
      Documents -> pdf pages seperated
      Vector store -> embedding for each Collection(pdf file) + text + metadata +...
      Vector index -> special data structure for stored embeddings to make queries faster

